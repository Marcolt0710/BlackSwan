export interface TimelineStep {
  number: string;
  title: string;
  description: string;
  details: string;
}

export interface GalleryItem {
  id: string;
  image: string;
  title: string;
  styleClass: string;
}
